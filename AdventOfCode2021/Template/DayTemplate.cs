namespace AdventOfCode2021
{
    public class DayTemplate : BaseDay
    {
        public override string ExecutePartOne(string file)
        {
            IEnumerable<string> input = ReadLines(nameof(DayTemplate), file);

            return string.Empty;
        }

        public override string ExecutePartTwo(string file)
        {
            IEnumerable<string> input = ReadLines(nameof(DayTemplate), file);

            return string.Empty;
        }
    }
}
